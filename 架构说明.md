# 文章与播客智能提炼总结工具 - 架构说明

## 核心架构变更

### 微服务架构
采用 **Go + Python** 双后端微服务架构：

1. **Go 主服务** - 负责业务逻辑和数据管理
   - 用户认证、授权
   - 数据库 CRUD
   - 文件上传/下载
   - API 网关
   - WebSocket 连接

2. **Python AI 服务** - 专注 AI 模型调用
   - 多模型适配器（OpenAI/Claude/Gemini）
   - 文本总结和分析
   - 语音转文字（Whisper）
   - 向量嵌入和相似度计算
   - Celery 异步任务处理

### 多模型支持

#### 支持的 AI 提供商
- **OpenAI**: GPT-4, GPT-4o, GPT-3.5-turbo
- **Anthropic**: Claude 3.5 Sonnet, Claude 3 Opus
- **Google**: Gemini Pro, Gemini Pro Vision
- **本地模型**: Ollama（可选）

#### 模型切换机制
- 用户可配置多个 API Key
- 每次总结时可选择使用的模型
- 支持设置默认模型
- API Key 加密存储
- 历史记录显示使用的模型

### 技术栈总结

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 主后端 | Go (Gin/Fiber) | 高性能、并发处理 |
| AI 服务 | Python (FastAPI) | AI 生态丰富 |
| 数据库 | PostgreSQL | 关系型数据 |
| 缓存 | Redis | 会话、缓存、队列 |
| 存储 | MinIO / S3 | 文件对象存储 |
| 任务队列 | Celery | 异步任务处理 |
| 前端 | React + TypeScript | 现代化 UI |
| 容器化 | Docker Compose | 一键部署 |

## 服务通信

### 同步调用
```
前端 → Go API → Python AI 服务 → AI 模型 API
```

### 异步调用（音频转写）
```
前端 → Go API → Redis 队列 → Celery Worker → Whisper API
                    ↓
              WebSocket 推送进度
```

## 数据流

1. **用户注册/登录** → Go 服务处理
2. **配置 AI 模型** → Go 存储加密 API Key
3. **提交文本总结** → Go 接收 → Python AI 服务 → 选定的 AI 模型
4. **上传音频** → Go 存储文件 → Redis 队列 → Celery 异步转写
5. **查看历史** → Go 从数据库读取
6. **交叉分析** → Python 计算向量相似度

## 部署方式

### 开发环境
```bash
docker-compose up -d
```

### 生产环境
- Go 服务：多实例负载均衡
- Python AI 服务：多实例 + Celery Worker 集群
- PostgreSQL：主从复制
- Redis：哨兵模式
- MinIO：分布式部署

## 扩展性

### 水平扩展
- Go 服务：无状态，可任意扩展
- Python AI 服务：无状态，可任意扩展
- Celery Worker：可根据任务量动态扩展

### 添加新 AI 模型
只需在 Python 服务中添加新的适配器类：
```python
class NewModelAdapter(AIModelAdapter):
    async def summarize(self, text: str, options: dict) -> dict:
        # 实现新模型的调用逻辑
        pass
```

## 成本优化

1. **缓存策略**: 相同内容不重复调用 AI
2. **模型选择**: 用户可选择性价比高的模型
3. **批量处理**: 多个任务合并调用
4. **本地模型**: 支持 Ollama 降低成本

## 安全性

1. **API Key 加密**: 使用 AES-256 加密存储
2. **JWT 认证**: 无状态身份验证
3. **HTTPS**: 生产环境强制 HTTPS
4. **速率限制**: 防止滥用
5. **输入验证**: 防止注入攻击

## 下一步

请审阅 `DEVELOPMENT_PLAN.md` 获取完整的开发计划，包括：
- 8 个开发阶段详细说明
- 数据库设计
- API 端点设计
- Docker 配置
- 环境变量配置

确认后即可开始阶段 1 的开发工作。
